<UserControl x:Class="SophonDownloader.UpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="UPDATES" Foreground="{DynamicResource AccentBrush}" FontSize="12" FontWeight="SemiBold"/>
            <TextBlock Text="Check for Updates" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="32" FontWeight="SemiBold" Margin="0,4,0,0"/>
            <TextBlock Text="Check, download, and install application updates without leaving Sophon Downloader." Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,7,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Margin="0,24,0,0" Background="{DynamicResource PanelStrongBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="24">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Text="Current version" Foreground="{DynamicResource MutedTextBrush}" FontSize="10"/>
                    <TextBlock x:Name="CurrentVersionText" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="20" FontWeight="SemiBold" Margin="0,3,0,0"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Margin="0,20,0,0">
                    <TextBlock x:Name="StatusText" Text="Ready to check for updates." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12" TextWrapping="Wrap"/>
                    <ProgressBar x:Name="UpdateProgressBar" Margin="0,12,0,0" Visibility="Collapsed"/>
                </StackPanel>
                <TextBox x:Name="ReleaseNotesTextBox" Grid.Row="2" Margin="0,18,0,18" IsReadOnly="True" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Background="{DynamicResource InputBackgroundBrush}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                <Button x:Name="ActionButton"
                        Grid.Row="3"
                        Content="Check for Updates"
                        Height="38"
                        HorizontalAlignment="Right"
                        MinWidth="170"
                        Padding="16,0"
                        Background="{DynamicResource PrimaryBrush}"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        Click="ActionButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"
                                                  RecognizesAccessKey="True"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
